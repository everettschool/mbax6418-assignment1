# Step 5 Grader — Primary-emotion detection

Verified as passing: prompt v2 asks for both sentiment and one of the 8 emotions, and the parser accepts only those 8 (`src/classify.py:92`). The word list is the official NRC lexicon, scored on the 8 emotions only (asserted). It makes no model calls and runs over the existing predictions. Both emotions are kept per row. Agreement is reported three ways, and the cross-tab and per-review hits are saved and shown on the dashboard. ISSUES.md has 10 Step 5 entries. I re-ran `src/nrc_emotion.py` on a copy (`reviews/step5_grader_scratch/first100_v2`), and every row and every metric came out byte-identical ("rows identical: True / metrics identical: yes"). Sentiment flips from v1 to v2: `[]`. The numbers in `step5_analysis.md` (20/100, 20/85, 20/38, TIE 47, NONE 15, 45/47, crosstab cells, 8 "easy" NONE rows, gift 104) all match the saved files.

MAJOR — reviews/step5_analysis.md "Agreement, three ways" (and ISSUES.md "[Step 5] Word-list emotion mostly fails to decide") — The main interpretation has no skew baseline. The claim that "in 45 of the 47 ties the LLM's emotion is one of the tied emotions … the low headline agreement is therefore mostly the word list failing to decide, not the two methods pointing in opposite directions" does not hold up. A constant answer of "joy" does exactly as well on ties and better on agreement, so the 45/47 figure says nothing about the LLM. This is the same trap the assignment warns about for sentiment ("don't let that lopsidedness fool you"). The conclusion "when the word list does produce a single answer, the two methods agree about half the time" is also unqualified: always-joy agrees more often. — Evidence: Python over `runs/first100_v2/predictions.jsonl` printed `ties containing joy: 45 / 47`, `constant-joy agreement all rows: 22 /100`, `constant-joy agreement excl NONE+TIE: 22 / 38`, `LLM agreement excl NONE+TIE: 20 / 38`. Only 3 LLM answers inside ties are not joy (ids 14, 50, 83). A grep for "baseline|always.*joy|constant" in step5_analysis.md and ISSUES.md finds no emotion baseline. — Suggested fix: add an always-joy (majority LLM emotion) baseline to `emotion_metrics.json`: all rows, excluding NONE+TIE, and tie containment. Rewrite the tie and agreement conclusions against it: on this 93%-positive batch, agreement is no better than a constant joy answer. Also consider agreement restricted to the non-joy LLM rows. Mirror the correction in ISSUES.md.

MINOR — reviews/step5_analysis.md, divergent case 4 (review_id 17) — The analysis says the single anger word "complaint" is "outvoted 9 to 1 by topic words", and its hit list sums to only 8. The 9th anticipation point comes from "pretty", a positive evaluative word, not a topic word. — Evidence: `step5_emotion_explore.txt` hits for id 17 include `['pretty', 'pretty', ['anticipation', 'joy', 'trust']]`. The analysis lists "gift ×3, recipient ×2, time, times → time, mail", which is 8. — Suggested fix: list "pretty" among the hits and say "8 topic words plus 'pretty'".

MINOR — reviews/step5_analysis.md "Where the word list gives no answer — TIE" — The analysis does not say that 9 of the 47 TIE rows (and 9 of the joy→TIE 44) are the duplicate "Good Product"/"Good product" rows. That group was already known from Step 2, and it inflates the tie count. — Evidence: `TIE rows that belong to duplicate groups: [20, 21, 22, 23, 24, 25, 26, 27, 28] 9`; on unique (title, text) rows: `unique rows 93 agree 20 TIE 40`. — Suggested fix: add one line disclosing the duplicates and the de-duplicated TIE count (40 of 93).

MINOR — reviews/step5_analysis.md "Why they differ" — Two real reasons for divergence are left out. (a) Negation: the word list counts negated emotion words as if they were affirmed. (b) The prompt's own glosses steer the LLM's labels. For example, v2 defines trust as "dependable, safe, or convenient", and 8 of the 9 LLM trust rows are "easy/convenient" reviews, which the lexicon cannot tag. Part of the trust→NONE 5 cell therefore comes from how the prompt defines trust, not from the LLM reading the review better. — Evidence: 15 hit tokens follow a negator within 3 tokens, e.g. `(64, 'but nothing is good')`, `(76, 'is not to love')`, `(4, 't able to cover')`, `(46, 'card nothing to show')`. LLM trust rows are 14 "Reloading is easy…", 31 "So easy", 34 "So easy to use", 49 "Really easy to load", 55 "Simple and easy.", 56 "…a time saver", 78 "Easy to use", 97 "easy to use and very convenient"; `prompts/sentiment_v2.txt`: "trust: relies on it, finds it dependable, safe, or convenient". — Suggested fix: add both as grounded limitations with row ids. Avoid attributing LLM trust labels only to whole-review reading.

MINOR — reviews/step5_analysis.md divergent-case selection — The largest divergent cell, joy→anticipation (8 of 18 divergent rows), gets only one explained example (93), and the analysis itself calls that one "not decisive". The actual driver in that cluster is anticipation-only topic words ("mail"/"mailed", "time", "recipient", "store"), and none of those rows is walked through. — Evidence: `crosstab_cells` `joy->anticipation: 8`. Explore output: id 9 single hit `mail (anticipation)`; id 41 `perfect`, `mailed → mail`; id 16 `gift`, `recipient`; id 71 `time`. — Suggested fix: explain one clean joy→anticipation row (e.g. 9 or 41) and state how many of the 8 are decided by mail/time/recipient/store.

MINOR — dashboard emotion section (dashboard/template.html renderEmotion; screenshot dashboard/screenshots/drafts/step5_emotion_section.png) — (a) In the cross-tab, the "anticipation" column header breaks mid-word into "anticipati / on". The product bar is a polished dashboard, and this bug is not logged in ISSUES.md. (b) The page shows the 20.0% headline but not the tie context (47 ties, of which 45 contain the LLM's emotion) or any baseline, so a non-technical reader cannot see why the agreement figure is low. — Evidence: the screenshot's column header reads "anticipati" with "on" on a second line. A grep for `tie_rows` in template.html returns no match. — Suggested fix: use `hyphens:auto`/`overflow-wrap:normal`, abbreviations with full-name tooltips, or rotated headers. Add a tie/baseline note or tile fed from `emotion_metrics.json` with data-source tags. Log the header bug in ISSUES.md.

MINOR — src/nrc_emotion.py step 5 vs assignment "take the highest as the answer" — For 47 of 100 reviews the word list outputs "TIE" rather than an emotion, with no deterministic tie-break. The literal requirement is one highest emotion per review, and the headline agreement counts every tie as a disagreement. The choice is documented, but no strictly literal alternative is reported. — Evidence: `nrc_emotion_counts` TIE 47; docstring "Several share the highest total -> 'TIE'". — Suggested fix: keep TIE, and also report a documented deterministic tie-break (e.g. fixed emotion order or fewest-lexicon-tags) and its agreement, so the literal reading is covered.

MINOR — src/classify.py:237 / src/build_dashboard.py:43 (pipeline ordering) — Re-running `classify.py` on a run rewrites `predictions.jsonl` without the `nrc_*` fields and leaves the old `emotion_metrics.json` in place. `build_dashboard.py` then embeds the stale emotion metrics without any check, and `verify_dashboard.py` compares marks against that same stale file. — Evidence: classify.py line 237 `open(run_dir / "predictions.jsonl", "w")` writes rows built only from classify output. build_dashboard.py lines 43–45 load `emotion_metrics.json` whenever it exists. A grep for "stale|mtime|consistent|recompute" in build_dashboard.py and verify_dashboard.py finds nothing. — Suggested fix: when `emotion_metrics.json` exists, make build_dashboard (or verify_dashboard) require `nrc_emotion` in every row and recompute the agreement counts from rows. Alternatively, have classify.py delete the stale `emotion_metrics.json`.

MINOR — PROGRESS.md "Current step" — The header still reads "Step 5 — Primary emotion, two ways (starting)", while the status section below says most of the work is done. The Step 5 claim checks were written, but "view emotion screenshot" is still listed as remaining. — Evidence: quoted lines `## Current step` / `Step 5 — Primary emotion, two ways (starting)` and `Remaining: verify analysis claims (...), view emotion screenshot, Grader + Numbers Auditor`. — Suggested fix: update the current-step line and the remaining list.
