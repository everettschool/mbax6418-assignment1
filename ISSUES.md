# ISSUES.md — bugs, surprises, decisions, workarounds

One entry per item: **[Step N] title** — what happened — how it was resolved.
Entries marked *(addition)* are work beyond what the assignment requires.

## Step 0 — Environment

- **[Step 0] `pdftotext` not installed** — poppler is absent on this Mac. Extracted `docs/assignment.pdf` with `pypdf` inside the project virtualenv instead (allowed by the build prompt).
- **[Step 0] System Python is 3.9** — `/usr/bin/python3` is 3.9.6. Built the virtualenv (`.venv/`) with Homebrew Python 3.14.2 for current `openai`/`playwright` wheels.
- **[Step 0] `prompt.md` git-ignored** — the agent orchestration prompt sits in the project root and mentions the endpoint host by name. It is not a deliverable, so it is excluded via `.gitignore` to keep the host name out of the tracked tree.
- **[Step 0] Metadata kept in predictions files** — decision: carry `verified_purchase`, `helpful_vote`, `timestamp`, `asin`, `parent_asin`, and `images_count` (length of `images`) into every `predictions.jsonl` row, for slicing errors later (e.g., are unverified reviews harder?). Dropped `user_id` (a reviewer identifier with no analytic use here, and no reason to republish it) and the raw `images` URLs (bulky). None of these fields is ever passed to the model; `build_messages` accepts only `title` and `text`.
- **[Step 0] Inclusion rule** *(addition)* — rows whose `text` is empty after stripping whitespace are excluded from every run. 49 of 152,410 rows fail it (by rating: 1★ 18, 2★ 3, 3★ 1, 4★ 1, 5★ 26). The assignment says "first 100 rows"; `batch_100` is defined as the first 100 `review_id`s that pass the rule. In practice none of rows 0–99 fail it, so `batch_100` is exactly review_ids 0–99 (see Step 2).
- **[Step 0] Model is a "thinking" model; first call returned no answer** — `cyankiwi/Qwen3.6-35B-A3B-AWQ-4bit` (the only model `/v1/models` offers) spent all 200 `max_tokens` on a hidden `reasoning` field and returned `content: null`, `finish_reason: length`. Passing `extra_body={"chat_template_kwargs": {"enable_thinking": false}}` (a vLLM chat-template switch) made it answer `LABEL=POSITIVE` directly with 0 reasoning tokens in 0.5 s. Decision: every classification call disables thinking, so `max_tokens` can stay small as the parse policy requires; the setting is recorded in each `run_meta.json`. `temperature=0` was accepted without error.
- **[Step 0] Imbalance confirmed at the source** — full file: 5★ 84.1%, 4★ 4.4%, 3★ 2.1%, 2★ 1.2%, 1★ 8.1% (`runs/rating_distribution.json`). Binary truth (≥4 positive) is therefore ~88.5% POSITIVE across the file, so a model that always says POSITIVE scores ~88% before doing anything useful.

## Step 1 — Structured prompt, binary

- **[Step 1] Indirect rating leak through auto-generated titles** — while reading spot-check candidates, titles such as "Five Stars", "Three Stars", "One Star" turned up. Counting over the 152,361 included rows: 31,868 titles (20.9%) are exactly "<One–Five> Star(s)" (1★ 684, 2★ 139, 3★ 428, 4★ 1,653, 5★ 28,964), and the number equals the rating in 31,838 of them. They are Amazon's auto-filled titles, i.e. the rating itself. Passing them would break "the model never sees the rating". Fix: `build_messages` blanks a title (or a text — 13 texts are only such a phrase) that fully matches `^(one|two|three|four|five) stars?$`; the prompt says either field may be empty. The stored `title`/`text` in predictions stay original; `verify_leak.py` rebuilds the messages and asserts no such phrase was sent. Done before any run used `sentiment_v1.txt`, so the prompt was still editable.
- **[Step 1] Residual path, not closed: customers who mention stars in their own words** — 909 included texts say things like "5 stars" or "one star because…" inside longer text. That is the customer's review content, which the assignment tells us to classify, so it is left intact and disclosed rather than rewritten. `verify_leak.py` counts these per run.
- **[Step 1] Parser self-test caught a code-fence bug** — the first fence-stripping regex (`^`+[a-zA-Z]*`) treated `LABEL` in a single-backtick reply `` `LABEL=POSITIVE` `` as a fence language tag and deleted it, so a valid answer would have been counted FAIL. Fixed: a line that is only a fence (```` ``` ```` / ```` ```text ````) is skipped; other lines have backticks trimmed from both ends. `src/test_classify.py` covers both forms.
- **[Step 1] `openai` 3.x uses `httpx2`** — the self-test's `import httpx` failed; the SDK now depends on `httpx2`. Switched the import.
- **[Step 1] `verify_leak.py` crashed on a relative extra-file path** — `Path.relative_to` needs absolute paths; it also left a temp file in `reviews/`. Now resolves the path and writes the temp file under git-ignored `cache/`; the stray file was deleted.
- **[Step 1] Spot check 10/10** — 5 clearly positive and 5 clearly negative reviews picked by reading only (review_ids 1043, 1113, 1197, 1218, 1232 / 1453, 1624, 2473, 3637, 4511, all outside `batch_100`); the model's labels matched reading on all 10, and the ratings revealed afterward (five 5★, five 1★) agreed too. Caveat: picked for being obvious, so this proves the plumbing, not accuracy. Evidence: `reviews/step1_spotcheck.txt`, `.json`.

## Step 2 — Score against the rating (binary)

- **[Step 2] batch_100 equals review_ids 0–99** — the inclusion rule excluded 0 rows in the batch (`run_meta.selection_info.rows_skipped_by_inclusion_rule` = 0), so the Step 0 "deviation" from "first 100 rows" has no effect on this run.
- **[Step 2] Duplicate reviews inside batch_100** — 9 rows fall into two identical groups: "Good Product" ×7 (review_ids 20–24, 27, 28) and "Good product" ×2 (review_ids 25–26), i.e. 7 rows repeat an earlier row. Identical rows share a cache key (model + prompt + title + text), so only the first of each group was sent; the run used 90 API calls with 10 cache hits (3 from the Step 2 preview + 7 repeats). Kept as-is: they are genuine rows under the inclusion rule, and dropping them would change the batch definition. They pad the easy-POSITIVE share of the batch, which is noted beside the results.
- **[Step 2] Headline accuracy is mostly the skew** — `first100_v1` scored 97.0% (97/100), but the majority-class baseline is 93.0% (93 POSITIVE / 7 NEGATIVE truth), so the model beats "always POSITIVE" by 4 reviews. The batch is also more lopsided than the file (7% NEGATIVE vs 11.5% file-wide). NEGATIVE recall is 6/7 and precision 6/8, too few rows for a stable per-class estimate. Written up with cell counts in `reviews/step2_analysis.md`.
- **[Step 2] Two errors are instruction-following failures, not rule gaps** — review_id 46 ("Love it!!" / "This is an online gift card nothing to show sorry🤪") and review_id 17 ("the recipient loved it ! I have one complaint…") were both predicted NEGATIVE, although the v1 prompt's own rules (classify from the title when the text is filler; NEGATIVE only if the card was unusable, money was lost, or they would not buy again) point to POSITIVE for both. My first write-up blamed the rules; the Grader (round 1, MAJOR) showed the rules say the opposite, and the analysis was corrected. v1 is immutable and already scored, so it was not tuned on the evaluation batch.
- **[Step 2] 3★ counted NEGATIVE by the binary rule** — review_id 98 (3★, "Very easy to use. I wish I knew about it earlier") is scored as a model error although its words are positive. Kept per the assignment's rule (≥4 positive, else negative); flagged as a truth-definition artifact to revisit in Step 6.
- **[Step 2] Reviewer round 1** — Grader: 1 MAJOR, 4 MINOR; Numbers Auditor: 0 mismatches over 106 checks, 1 MINOR (duplicate wording). All fixed in `reviews/step2_analysis.md`, this file, and PROGRESS.md; none rejected.

## Step 3 — Dashboard

- **[Step 3] Charts built from HTML/CSS marks, not SVG** — the build prompt prefers inline SVG. I used flex/grid `div` marks instead: text labels never get stretched by an SVG `viewBox`, the layout reflows on narrow screens, and every fill is still a CSS custom property from the single `:root` block, so recoloring works the same. Every mark carries `data-run`, `data-metric`, `data-value`, and `data-label`, and has `min-width/min-height: 2px` so a non-zero value cannot collapse.
- **[Step 3] Palette chosen by running the dataviz validator, not by eye** — class identity uses blue `#2f5aa8` (POSITIVE), vermilion `#c8502e` (NEGATIVE), and violet `#6e56b8` (UNPARSED): all checks pass, worst adjacent CVD ΔE 20.6. NEUTRAL is a deliberate warm gray `#8f8a7e` (fails only the chroma floor, by design, as a neutral midpoint; always shown with a text label). The first right/wrong pairs, green vs pink (`#237a57`/`#c23a6b` and two variants), **failed** colorblind separation (deutan ΔE 2.8–5.4). Switched to slate `#3e4a57` for right vs amber `#d9822b` for wrong (CVD ΔE 26.7): mistakes are the loud color and correct answers recede. Amber is under 3:1 contrast against the surface, so every amber mark also carries a visible count.
- **[Step 3] Nested children rendered as "[object HTMLSpanElement]"** — the first headless screenshot showed the precision footnote printing object names: the DOM helper flattened children one level only. Fixed with `flat(Infinity)`; caught by looking at the screenshot, not by any assertion, which motivates the text checks in `verify_dashboard.py` (Step 4).
- **[Step 3] Raw review text contains HTML** — the dataset text includes `<br />` and entities such as `&#34;`. The model received the text exactly as stored. For display only, the page renders them as line breaks and characters (still inserted as text, never as HTML). The saved predictions are unchanged.
- **[Step 3] Build guard against network URLs** — `build_dashboard.py` refuses a template containing `http://` or `https://`, so the page cannot fetch anything. Headless load of the built page recorded 0 non-file requests and 0 console errors.
- **[Step 3] Numbers Auditor round 1: 0 mismatches, 2 MINOR, both fixed** — `reviews/step3_numbers_auditor.md` compared 62 metric fields, 56 tagged numbers and 14 chart marks in headless Chromium against its own recomputation: all equal. (1) The table's "of 100 reviews" total was computed in the page (`rows.length`) with no `data-source`; it is now `metrics.json#n_rows` with full data attributes, and the shown count carries `data-live-count` for Step 4's filter checks. (2) The run blurb in `manifest.json` repeated run facts by hand ("first 100", the star rule); the blurb is now built from `run_meta.json` fields (`n_rows`, `selection_rule`, `prompt_file`, `label_scheme`) as tagged spans, and `build_dashboard.py` refuses to build if a tab label's number differs from `n_rows` or its class wording differs from `label_scheme`. None rejected.

## Step 4 — Interactive filtering

- **[Step 4] Filter design** — four combinable filters above the review table: Result (all / matches / mistakes), Answer key class, Model's predicted class (including Unparsed), and free-text search, plus Reset and a live "Showing N of M reviews" count. Search is case-insensitive substring over title + text, matching either the stored text or its display form (so `4"` finds a review stored as `4&#34;`). Filters reset when switching runs.
- **[Step 4] Confusion cells filter the table** *(addition)* — clicking (or Enter on) any non-zero heatmap cell sets the Answer key and Model filters to that cell and scrolls to the table, so each error count in the matrix leads straight to the reviews behind it.
- **[Step 4] `src/verify_dashboard.py`** *(addition — the assignment asks to check page numbers "in the browser", this makes it mechanical)* — in headless Chromium it (A) resolves every `data-source` and compares file value, `data-raw`, and displayed text under the display rule (mirroring JavaScript `toFixed` rounding exactly with `Decimal` ROUND_HALF_UP on the binary value, not Python's half-even `format`); (B) checks every chart mark's value against its saved field and that non-zero marks render ≥ 2px in both dimensions; (C) for each filter and combination, compares the *set* of visible review_ids (not just the count) with the rows computed from `predictions.jsonl`, and the count text and `data-live-count`; (D) fails on console errors, network requests, > 1 MB, or any untagged percentage outside the table.
- **[Step 4] Verifier passes, and was shown to fail when it should** — on the built page: 294 checks, 0 failures (60 metric numbers, 14 marks, 17 filter cases; `reviews/step4_verify_dashboard.txt`). A verifier that only ever passes proves little, so three deliberately broken copies of the page (kept in git-ignored `cache/mutations/`) were run through it: a wrong embedded accuracy (0.97→0.96) → exit 1, 4 failures; inverted match/mistake filter logic → exit 1, 15 failures; chart marks forced to 0px → exit 1, 8 failures ("value 93 renders at 0.00x16.00px"). All three caught (`reviews/step4_verify_mutation.txt`). Clicking the Positive→Negative heatmap cell set both filters and showed "Showing 2 of 100 reviews", matching `metrics.json` `confusion_cells` = 2.
- **[Step 4] Tooltip stayed on screen after a heatmap click** — seen in the filtered-state screenshot: the cell's tooltip ("2 · Stars: Positive → model: Negative") remained over the review table after the click scrolled the page, because tooltips only hid on pointer movement or focus loss. Fixed in Step 5 by hiding the tooltip on any click or scroll.

## Step 5 — Primary emotion, two ways

- **[Step 5] Prompt v2 is a new file; v1 untouched** — `prompts/sentiment_v2.txt` keeps every v1 sentiment rule verbatim and adds task two: one primary emotion from exactly anger, anticipation, disgust, fear, joy, sadness, surprise, trust, in lowercase, each with a one-line gift-card-context gloss. Policies chosen: judge the *reviewer's* emotion, not the recipient's (unless the reviewer shares it); a flat, matter-of-fact review still gets the closest of the eight, because the assignment asks for one primary emotion and has no "none" option. Output line: `LABEL=<X>;EMOTION=<Y>`. Like v1, the prompt contains no digits and none of rating/star/stars/helpful/verified.
- **[Step 5] Adding the emotion task did not change sentiment on batch_100** — `first100_v2` has 0 rows whose sentiment prediction differs from `first100_v1`; the same three reviews are wrong (17 and 46 POSITIVE→NEGATIVE, 98 NEGATIVE→POSITIVE), 0 parse failures, 0 retries, 90 API calls + 10 cache hits. `verify_leak.py` passes for both runs (`reviews/step5_verify_leak.txt`).
- **[Step 5] LLM emotions are concentrated in one label** — on batch_100 the model answered joy 80, trust 9, anger 7, anticipation 3, disgust 1 (fear, sadness, surprise 0). With 93 positive reviews this is expected, but it means emotion agreement on this batch will mostly measure whether the word list also says joy.
- **[Step 5] Official NRC download refused Python's default client (HTTP 406)** — `curl -I` on the official zip returned 200, but `urllib` with its default `Python-urllib` User-Agent got `HTTP Error 406: Not Acceptable`, and the NRCLex fallback was not installed, so the first `get_nrc.py` run crashed. Fix: the request now carries a User-Agent that honestly names this script (`mbax6418-assignment1-get_nrc/1.0 (python urllib)`), with a `curl` retry of the same official URL if it is still refused and NRCLex only as a last resort. The second run succeeded on the first client: `download_client: urllib`, official source used, no fallback needed.
- **[Step 5] Lexicon recorded, not committed** — NRC Emotion Lexicon v0.92 word-level file (`NRC-Emotion-Lexicon/NRC-Emotion-Lexicon-Wordlevel-v0.92.txt` inside the official zip). zip sha256 `4edcbd00…34e6`, word file sha256 `02c66154…535a`, 14,154 words, 4,454 with at least one of the 8 emotions; full values in `runs/nrc_lexicon_meta.json`. The zip and word file live in git-ignored `data/nrc/`; `nrc_emotion.py` refuses to run if the file's hash differs from the recorded one.
- **[Step 5] Lexicon license shapes what is committed** — the lexicon's bundled README (section IX, Terms of Use) allows free non-commercial research and educational use, requires citing Mohammad & Turney (2013), and forbids redistribution. So the lexicon files stay out of the repo, the README will cite the papers and link the lexicon home page, and a reader re-downloads it with `src/get_nrc.py`. Decision: each prediction row keeps `nrc_hits` (the review's own tokens, the lexicon word they matched, and that word's emotions), because the Step 5 comparison must be grounded in each review's lexicon hits and auditable. That is a small per-review excerpt tied to review text, not a copy of the lexicon; the full word list is never written to a tracked file.
- **[Step 5] Emotion agreement is no better than a constant "joy" answer** — LLM vs word list: 20/100 (20.0%) all rows, 20/85 (23.5%) excluding NONE, 20/38 (52.6%) excluding NONE and TIE. A constant answer of "joy" (the LLM's most common emotion) scores 22/100, 22/85, 22/38 against the same word list, so the LLM is 2.0 points below it overall and 5.3 points below where the word list has one winner; where the LLM did not say joy, the two agree on 1 of 20 reviews. The word list tied on 47 rows (9 of them in the duplicate "Good Product" groups; removing the 7 repeats leaves 40 ties among 93 unique reviews) and found nothing on 15. Written up in `reviews/step5_analysis.md`.
- **[Step 5] Reviewer round 1: 1 BLOCKER, 1 MAJOR, 8 MINOR — all fixed, none rejected** —
  - (Numbers Auditor, BLOCKER) my review-17 hit list omitted "pretty" and "showed → show", so "outvoted 9 to 1 by topic words" was wrong: 8 of the 9 anticipation points are topic words and 1 is "pretty". Hit list corrected in full.
  - (Grader, MAJOR) I had concluded that "in 45 of the 47 ties the LLM's emotion is among the tied ones", so low agreement was mostly the word list failing to decide. With no skew baseline that did not hold: a constant "joy" is also in 45 ties and agrees more often (22 vs 20). Added constant-answer baseline fields to `emotion_metrics.json` and a baseline sentence on the dashboard; conclusion rewritten and the old claim marked withdrawn.
  - (Grader, MINOR ×8):
    - disclosed that 9 of the 47 ties belong to the duplicate groups (7 of them repeats);
    - added negation (15 hits in 11 reviews: 5 rhetorical such as "who doesn't love", 6 real negations scored as affirmed, 4 where the negator does not govern the hit word) and the prompt's trust gloss (8 of 9 LLM trust answers are "easy/convenient" reviews) as causes;
    - explained the joy→anticipation cell (all 8 decided by one anticipation-only word: mail, time, recipient, store);
    - fixed the cross-tab header break ("anticipati/on" → "anticip." with the full name as tooltip);
    - added a documented tie-break alternative (`nrc_emotion_tiebreak`, 23/100);
    - `classify.py` now deletes a stale `emotion_metrics.json` and `build_dashboard.py` refuses to embed emotion metrics that don't match the rows;
    - updated PROGRESS.md.
  - (self-found while viewing the round-2 screenshot) the new dashboard context line said "the model's emotion is among the tied ones in 45" without the matching constant-"joy" count, which on its own repeated the withdrawn argument. It now also states that "joy" alone is among the tied emotions in 45 (`tie_rows_constant_in_tied_set`), so ties do not show the model reading emotion better.
- **[Step 5] Reviewer round 2: 2 BLOCKER, 9 MINOR — all fixed, none rejected** —
  - (Numbers Auditor, BLOCKER) ISSUES.md still said the LLM's "agreement advantage partly reflects a skewed batch" after round 1 showed it has none. Rewritten: 20/100 vs a constant "joy" at 22/100.
  - (Numbers Auditor, BLOCKER) PROGRESS.md quoted "1,309 checks" for `verify_dashboard.py`, but the saved log said 1,342. PROGRESS.md is now rewritten from the final saved log at the gate.
  - (Grader + Auditor, MINOR) "Most negations are rhetorical" was false. Of the 15 negated hits, 5 are rhetorical, 6 are real negations scored as affirmed, and in 4 the negator does not govern the hit word. The analysis now lists all 15, and the unsupported "the LLM reads them in context" was dropped.
  - (Grader, MINOR) The tie-break alternative had no baseline and hid its order bias. New fields `tie_rows_broken_to` (43 of 47 ties → anticipation), `tiebreak_label_counts` and `tiebreak_constant_baseline` show that constant "joy" also scores 23/100 under it and constant "anticipation" 57/100. The analysis says the variant is an artifact of the order.
  - (Grader, MINOR) "9 duplicate ties" vs "40 of 93 unique" did not add up. Reworded: 9 ties sit in the duplicate groups, and 7 of those are removed repeats.
  - (Auditor, MINOR) The cheer tags in ISSUES.md were incomplete; corrected to anticipation/joy/surprise/trust.
  - (Grader + Auditor, MINOR) PROGRESS.md still described round 1 as open; rewritten at the gate.
  - (Grader, MINOR) The emotion section broke at a 400px viewport (bars and cross-tab clipped, headers "anticipdisgust"), and no screenshot of the final build had been viewed.
    - Fix: added `src/verify_dashboard.py` check (no sideways page scroll at 400px for every run), which immediately failed: 503px on `first100_v2`.
    - Cause: grid children keep `min-width: auto`, so the cross-tab's minimum width stretched its grid column past the screen.
    - Fixed with `min-width: 0` on grid children, a one-column `minmax(0, 1fr)` layout on narrow screens, and a horizontal scroll wrapper around the cross-tab.
    - Viewing the 400px screenshot of that fix showed the squeezed cross-tab headers running together ("anticip.disgust", "sadnesssur"); the cross-tab now keeps a 560px minimum width inside its scroll wrapper so headers stay apart and the wrapper scrolls instead.

## Step 6 — Three-class scoring with balanced sampling

- **[Step 6] Prompt v3 is a new file; v1 and v2 untouched** — `prompts/sentiment_v3.txt` keeps v2's emotion task verbatim and changes task one to three classes, with an explicit definition of each. NEUTRAL = "neither clearly satisfied nor clearly dissatisfied": lukewarm verdicts ("it's okay", "fine", "as expected"), reviews whose good and bad points roughly balance, and purely factual descriptions. The definition never mentions stars. Checked before first use: 0 digits, 0 of rating/star/stars/helpful/verified.
- **[Step 6] One v1/v2 rule deliberately reversed for three classes** — v1 and v2 said lukewarm reviews with no complaint are POSITIVE; that rule only made sense when there was no middle class. v3 sends them to NEUTRAL unless there is clear praise, and adds "the good and the bad carry about equal weight → NEUTRAL" to the mixed-review rule. This moves the prompt toward the three-class answer key, so any NEUTRAL result in Step 6 has to be read with that in mind rather than credited to the model alone.
- **[Step 6] Balanced sample: seed fixed, ids saved, redraw not needed to reproduce** — `balanced_150` = `random.Random(42).sample(sorted included review_ids of the class, 50)` for each of NEGATIVE (1–2★), NEUTRAL (3★), POSITIVE (4–5★), a fresh RNG per class. The 150 ids are saved in `runs/balanced150_v3/sample_ids.json`, and `classify.py` reads them from that file on any re-run instead of drawing again.
- **[Step 6] Balanced sample checked against the data file** (`reviews/step6_sample_check.txt`) — an independent re-draw with `random.Random(42)` reproduces the saved ids exactly; 50 per class, 150 unique, no overlap with batch_100. Pools: NEGATIVE 14,178, NEUTRAL 3,270, POSITIVE 134,913 included rows.
- **[Step 6] The balanced POSITIVE class contains no 4★ reviews** — star mix of the sample: 1★ 43, 2★ 7, 3★ 50, 4★ 0, 5★ 50. 4★ reviews are 6,691 of the 134,913 POSITIVE pool (under 5%), so 50 draws happened to include none. Consequence: the balanced run says nothing about 4★ reviews, which are the ones most likely to sit near NEUTRAL. Kept as drawn (changing the seed or stratifying by star after seeing results would be tuning the sample). How unusual: 63 of 1,000 seeds (0–999) also draw zero 4★ reviews into POSITIVE (`reviews/step6_round1_checks.txt`). Disclosed in the analysis and, since Step 6 round 1, on the dashboard: each run's description shows its own star mix from `metrics.json#sample_rating_counts`.
- **[Step 6] Sample quirks** — one pair of identical reviews (title "Five Stars", same text) shares a cache key, so the run had 4 cache hits (3 from the preview + 1 repeat) and sent 146 rows; with one parse retry (review 4880, below) that is 147 API calls. 26 of the 150 titles are star phrases ("Five Stars", "One Star"…) and were blanked for the model, against 4 of 100 in batch_100. Cause, measured rather than guessed: auto-filled star titles make up 22.7% of the POSITIVE pool, 13.1% of NEUTRAL and 5.8% of NEGATIVE, so a draw of 50 per class expects about 21 (median over 1,000 seeds 21, 5th–95th percentile 14–28); seed 42's 26 is high but ordinary, and batch_100 is the unusually low one. *(My first version of this entry said "because the balanced draw reaches older reviews"; that was never tested and is withdrawn.)* Seed 42 put 10 star-phrase titles into NEUTRAL (mean 6.6 over seeds; 117 of 1,000 seeds reach 10 or more); those 10 NEUTRAL rows scored 5 correct (4 predicted POSITIVE), the other 40 scored 12 correct (24 predicted NEGATIVE).
- **[Step 6] Balanced headline: 3★ reviews mostly do not get their own class** — `balanced150_v3` 73.3% (110/150) against a 33.3% majority baseline. NEUTRAL (3★) row: 17 predicted NEUTRAL, 25 NEGATIVE, 8 POSITIVE. Reverse direction: 4 NEGATIVE reviews predicted NEUTRAL; 2 POSITIVE predicted NEUTRAL. The model predicted NEGATIVE for 70 of 150. Written up with the rows in `reviews/step6_analysis.md`.
- **[Step 6] `first100_v3` (addition: same prompt as the balanced run, lopsided sample)** — 95.0% (95/100) against a 93.0% majority baseline; balanced accuracy 65.6%. Three-class truth on batch_100 is POSITIVE 93, NEUTRAL 2, NEGATIVE 5, so the NEUTRAL row rests on two reviews (one predicted POSITIVE, one NEGATIVE) and cannot say anything about 3★ reviews. 93 API calls, 7 cache hits (the duplicate "Good Product" rows again), 0 parse failures.
- **[Step 6] The Gift Cards file contains reviews of other products** — in the balanced NEUTRAL sample, review 140179 ("Disappointing": a device whose pages keep "not responding") and 143371 ("Pivots too much": a shaver) are 3★ reviews that are not about gift cards. Both were predicted NEGATIVE. Kept (the sample is fixed and they are genuine rows of the file), and flagged as dataset noise wherever the NEUTRAL→NEGATIVE cell is interpreted.
- **[Step 6] Customers' own star mentions reached the model in two rows** — review 121389 (3★, predicted NEGATIVE) says in its text "Cannot give 5 stars unless and until you create ability to send via e-delivery", and review 140676 (1★, predicted NEGATIVE, correct) has the title "I would give this 'zero stars'", which is not a pure star phrase and so was not blanked. This is the residual path recorded in Step 1: star counts inside the customer's own words, not auto-filled fields, so they were left in. `verify_leak.py`'s "3 reviews use a forbidden word" on this run = these two + 138151's title "Helpful Review" (not rating-related). *(My first version said one row; it had scanned texts only. Scanning the sent title and text together found the second.)*
- **[Step 6] The model broke the emotion vocabulary once, and a same-input retry gave a different answer** — review 4880's first reply was `LABEL=NEGATIVE;EMOTION=disappointment`, an emotion outside the allowed eight, so it failed to parse and was retried once with identical messages at temperature 0; the retry replied `LABEL=NEGATIVE;EMOTION=sadness`. Only row needing a retry in any run (`rows_needing_retry: 1`). Two consequences disclosed: temperature 0 on this endpoint is not fully deterministic, so the published numbers are reproduced from the committed predictions (and the cache), not guaranteed by re-querying; and the Wilson intervals cover sampling variation only, not this.
- **[Step 6] The new star-mix line rendered "[object HTMLSpanElement]", and 3,694 passing checks did not notice** — each run description gained "Stars in this sample: 1★ … 5★ …" built as nested arrays passed straight to the browser's `replaceChildren`, which stringifies arrays (the page's own `el()` helper flattens them; the Step 3 version of this bug was the same mistake in another place). Every number check still passed because the tagged spans were created, just not inserted. Caught by reading the rendered text back. Fix: wrap the line in `el()`. Prevention: `verify_dashboard.py` now fails if any run's page text contains "[object", "undefined" or "NaN".
- **[Step 6] Reviewer round 1: Grader 2 MAJOR + 3 MINOR; Numbers Auditor 0 mismatches over 11,865 checks + 3 MINOR; Skeptic 2 MAJOR + 5 MINOR — all fixed, none rejected** —
  - (Skeptic, MAJOR) Balanced-sample precision and accuracy were presented as facts about the model: "when it says NEUTRAL it is usually right (73.9%)", "the honest measure: 73.3%", and a margin comparison of +2.0 → +40.0 points over two different baselines. Added a population re-weighting to `comparisons.json` (addition). Re-weighted to the file's 88.5% / 2.1% / 9.3% class mix, NEUTRAL precision would be about 14.5% and accuracy about 94.1%. The analysis now says recall and balanced accuracy are what the balanced sample measures well, while precision and accuracy depend on its forced mix. The margin comparison is replaced by balanced accuracy (65.6% → 73.3%). *(Superseded in round 2: batch_100's balanced accuracy rests on 2 NEUTRAL reviews and would be 82.3% with one more right, so the change is not a finding. Q1 now uses per-class recalls: the balanced run's recalls applied to batch_100's class counts predict 94.5% against the observed 95.0%.)* On balanced runs, the dashboard shows the re-weighted precision next to the sample precision.
  - (Grader + Skeptic, MAJOR) I claimed "at least 7 of the 25 are mixed reviews the model tipped to NEGATIVE against its instructions". Re-read in full against every v3 rule, 4 follow the prompt's own NEGATIVE rules: 88787 (the text decides), 60608 ("won't be using it again"), 53136 (promised $10 not received), 121389 (ends "Bad bad bad"). At most 3 are candidate failures (32735, 60681, 147336). Claim withdrawn and conclusions rewritten: most of the cell is the stars and the words disagreeing. *(Superseded in round 2: only 10 of the 25 had actually been re-graded. All 25 re-graded → 17 follow the prompt, up to 8 candidates; and "the stars and the words disagreeing" was circular, since the NEGATIVE rules are my own prompt policy — reframed as the prompt's policy vs the reviewers' ratings.)*
  - (Grader MAJOR + Skeptic MINOR) The page never showed that the balanced POSITIVE class has no 4★ reviews. New `metrics.json#sample_rating_counts` fields, and every run description now shows its star mix.
  - (Grader, Auditor, Skeptic, MINOR) Two reviews' own words mentioned stars, not one: 121389 in its text and 140676 in its title. The third `verify_leak.py` hit is "Helpful Review". Entry and analysis corrected.
  - (Grader + Skeptic, MINOR) Review 4880's out-of-vocabulary emotion and its retry were unlogged, and the retry shows temperature 0 is not deterministic. New entry above, plus a "Repeatability" section in the analysis.
  - (Auditor + Skeptic, MINOR) A Wilson interval for plain accuracy was labelled "balanced accuracy". Relabelled, with a note that it treats the stratified draw as a simple random one.
  - (Skeptic, MINOR) The explanation that the balanced draw "reaches older reviews" was untested, and seed 42's composition was not examined. 1,000-seed composition checks added (`reviews/step6_round1_checks.txt`); explanation replaced with measured pool shares.
  - (Skeptic, MINOR) The trust shift was called "expected". It is now reported by class: POSITIVE 10, NEUTRAL 11, NEGATIVE 1.
  - (Grader + Auditor, MINOR) PROGRESS.md quoted a stale check count; updated from the saved log.
  - The "gift in 17 of 22 anticipation answers" figure first came from the Skeptic's scratch output. I recomputed it from saved rows (`reviews/step6_round1_claim_checks.txt`) before citing it.
- **[Step 6] The dashboard printed a literal "null" on every non-balanced tab** — the round-1 re-weighting note was `null` for runs without re-weighted fields, and `null` passed straight to `replaceChildren` becomes the text "null" after the precision footnote ("…Negative 71.4%null"). It is the same class of bug as "[object HTMLSpanElement]", and the check added for that one (`[object` / `undefined` / `NaN`) did not include "null". Found by the round-2 Skeptic. Fix: only add the note when it exists. `verify_dashboard.py` now also fails on the whole word "null" in page text outside the review table body (customer text may legitimately contain it).
- **[Step 6] The rating-blanking rule is exact, and wider spellings exist in the file** — `STAR_TITLE` blanks only "<One–Five> Star(s)". Scanning all included rows found 103 other titles that are a star count in another spelling: POSITIVE 5★ 93 and 4★ 3 ("5 star", "4 stars", "5 stars!"), NEGATIVE 1★ 4 ("ZERO STARS!", "0 stars"), NEUTRAL 3★ 3 ("3 stars", "Three stars."). None appears in the title or text sent in the four saved runs (`reviews/step6_round2_checks.txt` §E), so no published number is affected. Not changed in `classify.py` (that would alter the committed runs' message construction); instead `verify_leak.py` now fails if any sent title or text matches `^\W*(zero|one|two|three|four|five|[0-5](\.\d)?)\s*-?\s*stars?\W*$`, so a future re-draw cannot pass the audit with such a leak.
- **[Step 6] An empty title in the prompt carries a weak, rating-derived signal** — no included row in the file has an originally empty title, so an empty TITLE always means "a star-phrase title was blanked", which happens at different rates by class (POSITIVE 22.7%, NEUTRAL 13.1%, NEGATIVE 5.8%). In the balanced NEUTRAL row, blanked rows were predicted POSITIVE 4 of 10 times versus 4 of 40 (Fisher exact one-sided p = 0.041). Their texts are short and positive, so the words may explain it fully; disclosed as a caveat rather than claimed either way. A cleaner design (untested) would replace blanked titles with a fixed neutral placeholder only if originally empty titles also existed; in this file they do not, so any marker would carry the same signal.
- **[Step 6] Reviewer round 2: Grader 5 MINOR; Numbers Auditor 0 value mismatches over 15,151 checks + 2 MINOR; Skeptic 4 MAJOR + 7 MINOR — all fixed, none rejected** —
  - (Skeptic, MAJOR) "3★ reviews mostly collapse into NEGATIVE: 25 of 50" overstated the assignment's hint: 25 of 50 is half, with a 36.6%–63.4% interval. It now reads "half… the largest destination; this sample cannot say whether that is a majority".
  - (Skeptic, MAJOR) "At most 3 of the 25" was an upper bound drawn from 10 re-graded rows. All 25 were re-graded against every v3 rule: 17 follow the prompt, up to 8 are judgment-call candidates (4880, 22082, 32735, 51332, 53136, 60681, 111780, 147336). The Grader's 53136 point is included.
  - (Skeptic, MAJOR) "The finding is mostly about the answer key" was circular, because the NEGATIVE rules the model followed are my prompt's policy, inherited from the binary prompt. Reframed as the prompt's policy vs the reviewers' 3★ ratings: which is right is not settled, and a prompt variant is listed as untested future work.
  - (Skeptic, MAJOR) Literal "null" on the page; entry above.
  - (Skeptic MINOR + Grader MINOR) The dashboard still led balanced runs with "+40.0 pts over that shortcut", and only precision was re-weighted. On equal-mix runs the verdict line now says the shortcut reflects the forced mix, and the note adds re-weighted accuracy (94.1%).
  - (Skeptic, MINOR) The +7.7-point balanced-accuracy change flips sign on one review; superseded, see the round-1 log.
  - (Skeptic, MINOR) "gift" was cited as the cause of anticipation answers. Removing it leaves 18 of 22 unchanged; the analysis now cites the carrying words (time, delivery, money, store) with rows 11674 and 119173.
  - (Skeptic, MINOR) The star-count variant path and the empty-title signal; entries above.
  - (Skeptic, MINOR) "Leans on seed 42" overstated a one-review effect (8 observed vs about 7.0 expected); reworded.
  - (Skeptic, MINOR) "In three of the four" named no rows; now names 31137, 49142, 136849.
  - (Grader, MINOR) Review 99795 is now flagged as a probable breach of v3's sarcasm rule, outside the 3★ row.
  - (Auditor, MINOR) "±13 points" did not fit the plain-accuracy interval (±7 over 150 rows); split by sample size.
  - (Grader + Auditor + Skeptic, MINOR) PROGRESS.md still carried the "balanced accuracy" Wilson label, the one-row star mention and a stale "Next action"; all corrected.
- **[Step 6] Five of the eight 3★ reviews predicted POSITIVE have short, plainly positive text** — four had a blanked "Three Stars" title: 43478 "Children loved them", 73030 "I like very good products.", 144067 "Good because I spent my gift card.", 146724 "good". The fifth, 134401, has the real title and text "Yas" / "Yas". With the auto-filled title correctly withheld, these errors are mostly the star rating and the words disagreeing, not the model misreading the words. (First draft of this entry said all five had a blanked title; corrected after re-reading the rows.)
- **[Step 6] The emotion baseline needed a stronger version** — on the balanced run the LLM's most common emotion is anger, and a constant "anger" agrees with the word list on only 4/150, a weak bar the LLM clears easily (16/150). The word list's own most common single answer (anticipation, 22) would beat the LLM. Added `best_constant_baseline*` fields (the single constant that agrees most with the word list) to `emotion_metrics.json`, `comparisons.json`, and the dashboard context line, so the emotion comparison is always shown against the harder bar.
- **[Step 6] A typed conclusion on the dashboard was false for a new run** — the emotion context sentence added in Step 5 ended with the fixed words "so ties do not show the model reading emotion better". That was true for batch_100 (LLM emotion in 45 ties, constant "joy" in 45) but false on `balanced150_v3`, where the LLM's emotion is in 37 of 67 ties and the constant "anger" in only 6. Caught by reading the rendered text after the build; `verify_dashboard.py` checks every number but cannot judge a sentence. Fix: the sentence now states both counts and draws no conclusion; interpretation lives in the analysis files, where it is reviewed per run. Lesson applied: no hand-written conclusions in the template, only numbers bound to fields. A follow-up scan of every template string for conclusion words (so/therefore/better/worse/shows/clearly/strong/weak/reliable/accurate/fails) found no others.
- **[Step 6] Changing only the `#run=` part of the URL did not switch runs** — my own read-back script loaded `#run=balanced150_v3`, then navigated the same page to `#run=first100_v2` and printed balanced-run numbers under the first100_v2 label: a hash-only change does not reload the page, and the dashboard read the hash only at load. `verify_dashboard.py` was unaffected (it switches runs by clicking tabs), but a reader editing a shared link would see the wrong run. Fix: the page now listens for `hashchange`. Read-backs now select runs by clicking tabs.
- **[Step 5] Observed word-list limitations, each tied to a row** — (a) the product word "gift" is itself tagged anticipation/joy/surprise and was hit 104 times, producing most ties; (b) sarcasm and idiom read literally: "A Bronx cheer for you!" → cheer = anticipation/joy/surprise/trust (review 51); (c) word sense ignored: "Hit button by mistake" → hit = anger (32), "One size fits all" → fits = anger (93); (d) payment vocabulary tagged trust outweighs complaint words (63); (e) coverage gaps: "easy" is not an emotion word, so 8 short "easy to use" reviews are NONE (e.g. 31, 78, 98), as are emoji-only reviews (45); (f) the documented s/es/ed/ing fallback misses "loved" (stem "lov"), dropping the only explicit feeling word in review 17. These are reported, not patched: the method is fixed by the build spec, and tuning it on the evaluation batch would bias the comparison.
- **[Step 5] LLM emotion has its own failures** — review 46 ("Love it!!" / "…nothing to show sorry🤪") got LLM emotion disgust, which fits neither part; the word list's joy is closer. The LLM also answered joy for 80 of 100 reviews, and its agreement with the word list (20/100) is below a constant "joy" answer (22/100): it has no agreement advantage on this batch.
